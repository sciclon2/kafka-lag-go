events {}

http {
    server {
        listen 0.0.0.0:80;

        location / {
            auth_basic "Restricted Access";  # Displayed in the login prompt
            auth_basic_user_file /etc/nginx/.htpasswd;
            proxy_pass http://prometheus:9090/api/v1/write;  # Change to correct endpoint

        }
    }
}

